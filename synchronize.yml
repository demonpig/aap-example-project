---

- name: Synchronize Playbook
  hosts: all
  tasks:
    - file:
        path: /tmp/{{ item }}
        state: directory
      loop:
        - a
        - b
        
    - file:
        path: /tmp/a/{{ item }}
        state: touch
      loop:
        - 1
        - 2
        - 3
    
    - synchronize:
        src: /tmp/a
        dest: /tmp/b
        delete: true
        checksum: true
      delegate_to: "{{ inventory_hostname }}"
